services:
  courier-gls:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - courier-net
    restart: unless-stopped

networks:
  courier-net:
    driver: bridge
