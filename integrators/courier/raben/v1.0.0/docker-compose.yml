services:
  courier-raben:
    build: .
    ports:
      - "8010:8000"
    env_file:
      - .env
    networks:
      - courier-net
    restart: unless-stopped

networks:
  courier-net:
    driver: bridge
