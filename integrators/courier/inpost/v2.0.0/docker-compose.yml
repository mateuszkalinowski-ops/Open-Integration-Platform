services:
  courier-inpost-int-2024:
    build: .
    ports:
      - "8002:8000"
    env_file:
      - .env
    networks:
      - courier-net
    restart: unless-stopped

networks:
  courier-net:
    driver: bridge
