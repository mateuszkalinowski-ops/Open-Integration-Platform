services:
  amazon-integrator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: amazon-integrator
    ports:
      - "8000:8000"
    volumes:
      - ./config:/app/config:ro
      - amazon-data:/app/data
    env_file:
      - .env
    environment:
      - AMAZON_LOG_LEVEL=INFO
      - AMAZON_SCRAPING_ENABLED=true
      - AMAZON_SCRAPING_INTERVAL_SECONDS=300
    restart: unless-stopped
    networks:
      - integrations

  amazon-integrator-dev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: amazon-integrator-dev
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
      - ./config:/app/config:ro
      - amazon-data-dev:/app/data
    environment:
      - AMAZON_DEBUG=true
      - AMAZON_LOG_LEVEL=DEBUG
      - AMAZON_SCRAPING_ENABLED=false
      - KAFKA_ENABLED=false
      - DATABASE_URL=sqlite+aiosqlite:///./data/amazon_dev.db
      - DATABASE_ENCRYPTION_KEY=
    entrypoint: ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    profiles:
      - dev
    networks:
      - integrations

  tests:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: amazon-tests
    volumes:
      - ./src:/app/src
      - ./tests:/app/tests
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./test.db
      - DATABASE_ENCRYPTION_KEY=
      - AMAZON_SCRAPING_ENABLED=false
      - KAFKA_ENABLED=false
    entrypoint: ["python", "-m", "pytest", "tests/", "-v", "--tb=short"]
    profiles:
      - test
    networks:
      - integrations

volumes:
  amazon-data:
  amazon-data-dev:

networks:
  integrations:
    driver: bridge
