apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: oip

resources:
  - ../../base

namePrefix: fedex-

commonLabels:
  app.kubernetes.io/name: fedex-integrator
  app.kubernetes.io/version: "1.0.0"
  category: courier
  system: fedex

patches:
  - target:
      kind: Deployment
      name: integrator
    patch: |
      - op: replace
        path: /spec/template/spec/containers/0/image
        value: your-registry.example.com/integrations/courier/fedex:1.0.0-uat

generatorOptions:
  disableNameSuffixHash: true
