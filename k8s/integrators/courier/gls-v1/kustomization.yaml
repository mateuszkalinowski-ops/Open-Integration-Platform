apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: pinquark-uat

resources:
  - ../../base

namePrefix: gls-

commonLabels:
  app.kubernetes.io/name: gls-integrator
  app.kubernetes.io/version: "1.0.0"
  category: courier
  system: gls

patches:
  - target:
      kind: Deployment
      name: integrator
    patch: |
      - op: replace
        path: /spec/template/spec/containers/0/image
        value: registry.pinquark.com/integrations/courier/gls:1.0.0-uat

generatorOptions:
  disableNameSuffixHash: true
