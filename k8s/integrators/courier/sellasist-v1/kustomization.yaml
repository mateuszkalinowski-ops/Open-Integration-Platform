apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: pinquark-uat

resources:
  - ../../base

namePrefix: sellasist-

commonLabels:
  app.kubernetes.io/name: sellasist-integrator
  app.kubernetes.io/version: "1.0.0"
  category: courier
  system: sellasist

patches:
  - target:
      kind: Deployment
      name: integrator
    patch: |
      - op: replace
        path: /spec/template/spec/containers/0/image
        value: registry.pinquark.com/integrations/courier/sellasist:1.0.0-uat

generatorOptions:
  disableNameSuffixHash: true
