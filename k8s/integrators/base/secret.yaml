apiVersion: v1
kind: Secret
metadata:
  name: integrator-secrets
  namespace: oip
  labels:
    app.kubernetes.io/part-of: pinquark
  annotations:
    config.kubernetes.io/must-override: "true"
    description: "Base secret template — values MUST be overridden via CI/CD or kustomize patches. Never deploy with placeholder values."
type: Opaque
stringData:
  DATABASE_ENCRYPTION_KEY: ""
  KAFKA_SASL_USERNAME: ""
  KAFKA_SASL_PASSWORD: ""
